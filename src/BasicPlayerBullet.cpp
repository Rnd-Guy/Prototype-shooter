#include "BasicPlayerBullet.h"

BasicPlayerBullet::BasicPlayerBullet(sf::RenderWindow * window, std::string type, double spawnX, double spawnY, double spawnSpeed, double param1, double param2, double param3, double param4, double param5) :
	Bullet(window, "player", 5, type, spawnX, spawnY, spawnSpeed, param1, param2, param3, param4, param5)
{
	//this->window = window;
	//x = spawnX;
	//y = spawnY;
	//radius = 3;
	//initialise();
	//calculatePath(type, spawnSpeed, param1, param2, param3, param4);
	//deleteFlag = false;
	//damage = 5;
	//damageBoss = true;
	//damagePlayer = false;

}